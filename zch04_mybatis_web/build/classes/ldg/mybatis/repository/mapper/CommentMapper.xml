<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ldg.mybatis.repository.mapper.CommentMapper1">
	
	<cache />

	<!--
	<select id="selectCommentByPrimaryKey" parameterType="long" resultMap="constructorResultMap"> 
	 -->
	<select id="selectCommentByPrimaryKey" 
	        parameterType="long" resultType="Comment">	 
		SELECT 
			comment_no AS commentNo,
			user_id AS userId,
			comment_content AS commentContent,
			reg_date AS regDate
		FROM ztbl_comment 
		WHERE comment_no = #{commentNo} 
	</select>
	
	<select id="selectCommentByCondition" 
			parameterType="hashmap" resultType="Comment">
		SELECT
			comment_no,
			user_id,
			comment_content,
			reg_date
		FROM ztbl_comment
		<if test="commentNo != null">
			WHERE comment_no = #{commentNo}
		</if>
	</select>
	
	
	<select id="selectCommentByConditionIf" 
			parameterType="hashmap" resultType="Comment">
		SELECT
			comment_no,
			user_id,
			comment_content,
			reg_date
		FROM comment
		<!-- ...145p.where 엘리먼트 : 하위 엘리먼트가 생성하는 내용이 있으면 
		             자동으로 WHERE 를 붙여줌. -->
		<where>
			<if test="commentNo != null">
				comment_no = #{commentNo}
			</if>
			<if test="user != null and user.userId != null">
				AND user_id = #{user.userId}
			</if>			
		</where>
	</select>
</mapper>